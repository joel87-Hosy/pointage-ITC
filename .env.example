# URL publique de l'application (Railway la fournit automatiquement)
# Exemple : APP_URL=https://pointage-production.up.railway.app
APP_URL=

# Connexion PostgreSQL (Railway la fournit automatiquement)
DATABASE_URL=

# Port (Railway le définit automatiquement)
PORT=3000
